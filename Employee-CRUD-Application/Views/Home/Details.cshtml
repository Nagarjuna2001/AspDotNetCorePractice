@model HomeDetailsViewModel;

@{ 
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Employee Details";
    var photoPath = "~/Images/" + (Model.Employee.PhotoPath ?? "loginIcon.png");
}

<div class="row justify-content-center" m-3>
    <div class="col-sm-8">
        <div class="card">
            <div class="card-header">
                <h1>@Model.Employee.Name</h1>
            </div>
        </div>

        <div class="card-body text-center">

            <img asp-append-version="true" src="@photoPath" />

            <h4>Employee ID : @Model.Employee.Id</h4>
            <h4>Email : @Model.Employee.Email</h4>
            <h4>EDepartment @Model.Employee.Department</h4>
        </div>
        <div class="card-footer text-center">
            <a asp-controller="home" asp-action="index" class="btn btn-primary">Back</a>
            <a asp-controller="home" asp-action="edit" asp-route-id ="@Model.Employee.Id" class="btn btn-primary">Edit</a>
            <a href="#" class="btn btn-primary">Delete</a>
        </div>
>    </div>
</div>



    @*<h1>Details.cshtml from views/home folder</h1>*@

    @*Accessing data passed using ViewData*@
    @*<h2>@ViewData["PageTitle"]</h2>

        @{
            var employee = ViewData["Employee"] as FetchingEmployeeDataWebApplication.Models.Employee;
        }
        <div>
            Name: @employee.Name;
        </div>
        <div>
            Name: @employee.Department;
        </div>
        <div>
            Name: @employee.Email;
        </div>*@

    @*Acessing data passed using viewbag
        <h3>@ViewBag.pagetitle</h3>

        <div>
            Name : @ViewBag.Employee.Name;
        </div>
        <div>
            Name : @ViewBag.Employee.Department;
        </div>
        <div>
            Name : @ViewBag.Employee.Email;
        </div>*@

    @*Acessing data passed using viewbag*@
    @*<h3>@Model.PageTitle</h3>
    <div>
        Name : @Model.Employee.Name
    </div>
    <div>
        Department : @Model.Employee.Department
    </div>
    <div>
        Email : @Model.Employee.Email
    </div>*@
